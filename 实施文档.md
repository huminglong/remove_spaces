# Word文档中英文空格清理工具实施文档

## 1. 项目概述

### 1.1 项目背景
在日常文档处理中，中英文混排的Word文档经常出现中英文之间多余空格的问题。这些多余空格会影响文档的美观性和专业性。本工具旨在自动识别并清理这些多余空格，同时保持英文单词间的正常空格。

### 1.2 项目目标
- 自动识别Word文档中的中英文边界
- 清理中英文之间的多余空格
- 保持英文单词间的正常空格
- 提供用户友好的图形界面
- 支持批量文档处理

### 1.3 技术栈选择
- **编程语言**: Python 3.7+
- **GUI框架**: PyQt5/PySide2
- **文档处理**: python-docx
- **构建工具**: PyInstaller

## 2. 系统架构设计

### 2.1 整体架构
```
┌─────────────────────────────────────────────────────────────┐
│                    用户界面层 (GUI)                        │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │ 主窗口     │  │ 进度显示      │  │ 详细信息表格    │  │
│  └─────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                   业务逻辑层                              │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │文档处理器   │  │文本分析器    │  │空格清理器      │  │
│  │(Document   │  │(TextAnalyzer)│  │(SpaceCleaner)   │  │
│  │ Processor)  │  │              │  │                 │  │
│  └─────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                      数据层                              │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐  │
│  │Word文档     │  │处理结果      │  │保存文件夹      │  │
│  │(.docx)      │  │(.docx)       │  │processed_docs  │  │
│  └─────────────┘  └──────────────┘  └──────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 模块划分
- **GUI模块**: 负责用户界面展示和交互
- **文档处理器**: 负责Word文档的读取和写入
- **文本分析器**: 负责文本内容分析和边界识别
- **空格清理器**: 负责执行具体的空格清理操作

## 3. Word文档解析方案

### 3.1 文档结构分析
Word文档(.docx)采用OpenXML格式，主要包含以下结构：
- **段落(Paragraph)**: 基本的文本单元
- **表格(Table)**: 包含行列结构的文本
- **文本块(Run)**: 具有相同样式的连续文本

### 3.2 解析策略
```python
# 文档结构提取
def extract_document_structure(docx_path):
    doc = Document(docx_path)
    structure = {
        'paragraphs': [],
        'tables': []
    }
    
    # 提取段落
    for para in doc.paragraphs:
        paragraph_data = {
            'text': para.text,
            'runs': extract_runs(para),
            'style': para.style.name
        }
        structure['paragraphs'].append(paragraph_data)
    
    # 提取表格
    for table in doc.tables:
        table_data = {
            'rows': [],
            'style': table.style.name
        }
        for row in table.rows:
            row_data = []
            for cell in row.cells:
                cell_data = {
                    'text': cell.text,
                    'paragraphs': extract_paragraphs(cell)
                }
                row_data.append(cell_data)
            table_data['rows'].append(row_data)
        structure['tables'].append(table_data)
    
    return structure
```

### 3.3 文本片段提取
将文档内容分解为可处理的文本片段：
```python
def extract_text_segments(structure):
    segments = []
    
    # 处理段落
    for para in structure['paragraphs']:
        if para['text'].strip():
            segment = {
                'type': 'paragraph',
                'content': para['text'],
                'runs': para['runs'],
                'original_para': para
            }
            segments.append(segment)
    
    # 处理表格
    for table in structure['tables']:
        for row_idx, row in enumerate(table['rows']):
            for col_idx, cell in enumerate(row):
                if cell['text'].strip():
                    segment = {
                        'type': 'table_cell',
                        'content': cell['text'],
                        'position': (row_idx, col_idx),
                        'original_cell': cell
                    }
                    segments.append(segment)
    
    return segments
```

## 4. 中英文边界识别算法设计

### 4.1 Unicode字符范围定义
```python
# 中文字符Unicode范围
CHINESE_RANGES = [
    (0x4E00, 0x9FFF),   # CJK Unified Ideographs
    (0x3400, 0x4DBF),   # CJK Extension A
    (0x20000, 0x2A6DF), # CJK Extension B
    (0x2A700, 0x2B73F), # CJK Extension C
    (0x2B740, 0x2B81F), # CJK Extension D
    (0x2B820, 0x2CEAF), # CJK Extension E
    (0x2CEB0, 0x2EBEF), # CJK Extension F
    (0xF900, 0xFAFF),   # CJK Compatibility Ideographs
    (0x2F800, 0x2FA1F)  # CJK Compatibility Ideographs Supplement
]

# 英文字符范围
ENGLISH_RANGES = [
    (0x0041, 0x005A), # 大写字母 A-Z
    (0x0061, 0x007A), # 小写字母 a-z
    (0x00C0, 0x00FF), # 拉丁字母扩展
]

def is_chinese_char(char):
    """判断字符是否为中文"""
    code_point = ord(char)
    for start, end in CHINESE_RANGES:
        if start <= code_point <= end:
            return True
    return False

def is_english_char(char):
    """判断字符是否为英文"""
    code_point = ord(char)
    for start, end in ENGLISH_RANGES:
        if start <= code_point <= end:
            return True
    return False
```

### 4.2 边界检测算法
```python
def detect_language_boundaries(text):
    """检测文本中的语言边界"""
    boundaries = []
    
    for i in range(len(text) - 1):
        current_char = text[i]
        next_char = text[i + 1]
        
        # 检测中英文边界
        if (is_chinese_char(current_char) and is_english_char(next_char)) or \
           (is_english_char(current_char) and is_chinese_char(next_char)):
            boundaries.append({
                'position': i + 1,
                'type': 'chinese_english_boundary',
                'left_char': current_char,
                'right_char': next_char
            })
    
    return boundaries
```

### 4.3 文本分析结果
```python
def analyze_text_content(text):
    """分析文本内容，识别语言类型和边界"""
    result = {
        'segments': [],
        'boundaries': [],
        'statistics': {}
    }
    
    # 按语言类型分割文本
    current_segment = ''
    current_type = None
    
    for char in text:
        if is_chinese_char(char):
            char_type = 'chinese'
        elif is_english_char(char):
            char_type = 'english'
        else:
            char_type = 'other'
        
        # 如果类型变化，保存当前片段
        if current_type is not None and char_type != current_type:
            if current_segment.strip():
                result['segments'].append({
                    'text': current_segment,
                    'type': current_type,
                    'length': len(current_segment)
                })
            current_segment = ''
        
        current_segment += char
        current_type = char_type
    
    # 保存最后一个片段
    if current_segment.strip():
        result['segments'].append({
            'text': current_segment,
            'type': current_type,
            'length': len(current_segment)
        })
    
    # 检测边界
    result['boundaries'] = detect_language_boundaries(text)
    
    # 统计信息
    result['statistics'] = {
        'total_segments': len(result['segments']),
        'chinese_segments': len([s for s in result['segments'] if s['type'] == 'chinese']),
        'english_segments': len([s for s in result['segments'] if s['type'] == 'english']),
        'boundaries': len(result['boundaries'])
    }
    
    return result
```

## 5. 空格处理逻辑实现

### 5.1 清理规则定义
```python
class SpaceCleaningRules:
    """空格清理规则定义"""
    
    # 需要清理的模式
    CLEAN_PATTERNS = [
        (r'([\u4e00-\u9fff])\s+([a-zA-Z])', r'\1\2'),     # 中文 + 空格 + 英文
        (r'([a-zA-Z])\s+([\u4e00-\u9fff])', r'\1\2'),     # 英文 + 空格 + 中文
    ]
    
    # 需要保留的模式
    KEEP_PATTERNS = [
        r'[a-zA-Z]\s+[a-zA-Z]',  # 英文单词间的空格
        r'\n\s*',                  # 行首空格（缩进）
        r'\s*\n',                  # 行尾空格
    ]
    
    # 特殊情况
    SPECIAL_CASES = {
        'code_block': r'`[^`]*`',      # 代码块
        'url': r'http[s]?://[^\s]+',  # URL
        'email': r'[^\s]+@[^\s]+',   # 邮箱
    }
```

### 5.2 智能清理算法
```python
class SmartSpaceCleaner:
    """智能空格清理器"""
    
    def __init__(self):
        self.rules = SpaceCleaningRules()
        self.processing_stats = {
            'total_segments': 0,
            'cleaned_segments': 0,
            'skipped_segments': 0,
            'spaces_removed': 0
        }
    
    def clean_text(self, text):
        """清理文本中的多余空格"""
        self.processing_stats['total_segments'] += 1
        
        # 检查是否为特殊情况
        if self.is_special_case(text):
            self.processing_stats['skipped_segments'] += 1
            return text
        
        # 分析文本结构
        analysis = analyze_text_content(text)
        
        # 如果没有中英文边界，跳过处理
        if analysis['statistics']['boundaries'] == 0:
            self.processing_stats['skipped_segments'] += 1
            return text
        
        # 执行清理
        cleaned_text = self.apply_cleaning_rules(text)
        
        # 验证清理结果
        if self.validate_cleaning(text, cleaned_text):
            self.processing_stats['cleaned_segments'] += 1
            self.processing_stats['spaces_removed'] += self.count_removed_spaces(text, cleaned_text)
            return cleaned_text
        else:
            # 如果验证失败，返回原文本
            self.processing_stats['skipped_segments'] += 1
            return text
    
    def apply_cleaning_rules(self, text):
        """应用清理规则"""
        cleaned = text
        
        # 应用清理模式
        for pattern, replacement in self.rules.CLEAN_PATTERNS:
            cleaned = re.sub(pattern, replacement, cleaned)
        
        return cleaned
    
    def is_special_case(self, text):
        """检查是否为特殊情况"""
        for case_name, pattern in self.rules.SPECIAL_CASES.items():
            if re.search(pattern, text):
                return True
        return False
    
    def validate_cleaning(self, original, cleaned):
        """验证清理结果的有效性"""
        # 检查是否意外删除了必要空格
        original_words = original.split()
        cleaned_words = cleaned.split()
        
        # 如果英文单词数量发生变化，可能有问题
        original_english_words = len([w for w in original_words if w.isalpha()])
        cleaned_english_words = len([w for w in cleaned_words if w.isalpha()])
        
        if original_english_words != cleaned_english_words:
            return False
        
        # 其他验证规则...
        return True
    
    def count_removed_spaces(self, original, cleaned):
        """计算移除的空格数量"""
        original_spaces = original.count(' ')
        cleaned_spaces = cleaned.count(' ')
        return original_spaces - cleaned_spaces
```

### 5.3 文档处理流程
```python
def process_document_segments(segments):
    """处理文档中的所有文本片段"""
    cleaner = SmartSpaceCleaner()
    processed_segments = []
    
    for segment in segments:
        original_text = segment['content']
        
        # 清理文本
        cleaned_text = cleaner.clean_text(original_text)
        
        # 创建处理结果
        processed_segment = {
            'original': original_text,
            'cleaned': cleaned_text,
            'type': segment['type'],
            'changes': {
                'spaces_removed': cleaner.count_removed_spaces(original_text, cleaned_text),
                'is_modified': original_text != cleaned_text
            }
        }
        
        # 如果是表格单元格，保留位置信息
        if segment['type'] == 'table_cell':
            processed_segment['position'] = segment['position']
            processed_segment['original_cell'] = segment['original_cell']
        
        processed_segments.append(processed_segment)
    
    return processed_segments, cleaner.processing_stats
```

## 6. 特殊情况处理

### 6.1 表格处理
```python
def process_table_content(table_data):
    """处理表格内容"""
    processed_table = {
        'rows': [],
        'changes': []
    }
    
    for row_idx, row in enumerate(table_data['rows']):
        processed_row = []
        for col_idx, cell in enumerate(row):
            # 处理单元格内容
            cell_segments = extract_cell_segments(cell)
            processed_cell_segments, cell_stats = process_document_segments(cell_segments)
            
            # 重建单元格内容
            cleaned_content = rebuild_cell_content(processed_cell_segments)
            
            processed_row.append(cleaned_content)
            
            # 记录变更
            if cell_stats['cleaned_segments'] > 0:
                processed_table['changes'].append({
                    'position': (row_idx, col_idx),
                    'original': cell['text'],
                    'cleaned': cleaned_content,
                    'stats': cell_stats
                })
        
        processed_table['rows'].append(processed_row)
    
    return processed_table
```

### 6.2 样式保持
```python
def preserve_text_formatting(original_runs, cleaned_text):
    """保持文本的原始格式"""
    # 分析原始runs的格式
    formatting_info = []
    for run in original_runs:
        formatting_info.append({
            'text': run.text,
            'bold': run.bold,
            'italic': run.italic,
            'underline': run.underline,
            'font': run.font.name,
            'size': run.font.size
        })
    
    # 根据清理后的文本重新分配格式
    new_runs = []
    current_pos = 0
    
    for format_info in formatting_info:
        original_text = format_info['text']
        # 在清理后的文本中找到对应部分
        if original_text in cleaned_text:
            start_pos = cleaned_text.find(original_text, current_pos)
            if start_pos != -1:
                # 创建新的run
                new_run = {
                    'text': original_text,
                    'format': format_info,
                    'position': (start_pos, start_pos + len(original_text))
                }
                new_runs.append(new_run)
                current_pos = start_pos + len(original_text)
    
    return new_runs
```

### 6.3 错误处理
```python
class DocumentProcessingError(Exception):
    """文档处理异常基类"""
    pass

class UnsupportedFormatError(DocumentProcessingError):
    """不支持的文档格式"""
    pass

class CorruptedDocumentError(DocumentProcessingError):
    """文档损坏异常"""
    pass

def safe_process_document(docx_path):
    """安全的文档处理函数"""
    try:
        # 验证文件存在
        if not os.path.exists(docx_path):
            raise FileNotFoundError(f"文件不存在: {docx_path}")
        
        # 验证文件格式
        if not docx_path.lower().endswith('.docx'):
            raise UnsupportedFormatError("仅支持.docx格式")
        
        # 尝试打开文档
        try:
            doc = Document(docx_path)
        except Exception as e:
            raise CorruptedDocumentError(f"文档损坏或无法读取: {e}")
        
        # 执行处理
        result = process_document(doc)
        
        return {
            'success': True,
            'result': result,
            'message': '文档处理成功'
        }
        
    except DocumentProcessingError as e:
        return {
            'success': False,
            'error_type': type(e).__name__,
            'message': str(e)
        }
    except Exception as e:
        return {
            'success': False,
            'error_type': 'UnknownError',
            'message': f'未知错误: {str(e)}'
        }
```

## 7. 测试策略制定

### 7.1 测试层次
1. **单元测试**: 测试各个模块的功能
2. **集成测试**: 测试模块间的协作
3. **系统测试**: 测试完整功能流程
4. **用户验收测试**: 验证用户需求满足度

### 7.2 测试用例设计

#### 7.2.1 基本功能测试
```python
test_cases = [
    # 基本中英文边界
    ("你好 hello", "你好hello"),
    ("hello 你好", "hello你好"),
    
    # 多个边界
    ("你好 hello world 世界", "你好hello world世界"),
    
    # 英文单词间空格保持
    ("你好 hello beautiful world", "你好hello beautiful world"),
    
    # 纯中文/纯英文
    ("只有中文", "只有中文"),
    ("only english here", "only english here"),
]

def test_text_analyzer():
    # 测试中英文边界识别
    analyzer = TextAnalyzer()
    result = analyzer.analyze_text("这是一个test文本")
    assert len(result) == 3
    assert result[0]['type'] == 'chinese'
    assert result[1]['type'] == 'english'
    assert result[2]['type'] == 'chinese'

def test_space_cleaner():
    # 测试空格清理逻辑
    cleaner = SpaceCleaner()
    result = cleaner.clean_spaces("这是一个 test 文本")
    assert result == "这是一个test文本"

def test_document_processor():
    # 测试文档处理
    processor = DocumentProcessor()
    result = processor.process_document("test.docx")
    assert result is not None
    assert len(result['segments']) > 0

def test_save_functionality():
    # 测试保存功能
    # 创建测试文件夹
    test_folder = "processed_documents"
    if not os.path.exists(test_folder):
        os.makedirs(test_folder)
    
    # 测试保存逻辑
    save_path = os.path.join(test_folder, "test_cleaned.docx")
    # 保存测试文档
    assert os.path.exists(save_path)
    # 验证保存到processed_documents文件夹
    assert "processed_documents" in save_path
    
def test_save_to_default_folder():
    # 测试默认保存文件夹功能
    app = DocumentProcessorApp()
    
    # 创建测试文档
    test_content = "这是一个 test 文档"
    result = {
        'cleaned_text': "这是一个test文档",
        'statistics': {'total_segments': 1, 'cleaned_segments': 1}
    }
    
    # 保存结果
    save_path = app.save_results(result)
    
    # 验证保存路径包含processed_documents
    assert "processed_documents" in save_path
    # 验证文件存在
    assert os.path.exists(save_path)
    # 验证相对路径显示
    assert save_path.startswith("processed_documents/")
```

#### 7.2.2 边界情况测试
```python
edge_cases = [
    "",                    # 空字符串
    "   ",                 # 只有空格
    "你好   hello",        # 多个连续空格
    "你好! hello?",        # 包含标点
    "hello\t你好\nworld",   # 特殊字符
]
```

#### 7.2.3 性能测试
- **大文档处理**: 测试大文件的处理性能
- **批量处理**: 测试多个文档的处理效率
- **内存使用**: 监控内存占用情况

### 7.3 自动化测试
使用pytest框架实现自动化测试：
```python
@pytest.mark.parametrize("input_text,expected", test_cases)
def test_space_cleaning(input_text, expected):
    cleaner = SpaceCleaner()
    result = cleaner.clean_text(input_text)
    assert result['cleaned_text'] == expected
```

### 7.4 用户验收测试
- **测试目标**: 验证系统是否满足用户需求
- **测试场景**:
  1. **场景1**: 处理中英文混排的学术论文
     - 输入: 包含中英文摘要的论文
     - 期望输出: 中英文之间空格被正确清理
     - 验收标准: 清理准确率 > 95%
  
  2. **场景2**: 处理技术文档
     - 输入: 包含代码片段的技术文档
     - 期望输出: 代码片段中的空格保持不变
     - 验收标准: 代码片段不被误处理
  
  3. **场景3**: 批量处理多个文档
     - 输入: 10个不同的Word文档
     - 期望输出: 所有文档都被正确处理
     - 验收标准: 处理成功率 = 100%
  
  4. **场景4**: 保存功能测试
     - 输入: 处理完成的中英文文档
     - 期望输出: 文档自动保存到`processed_documents`文件夹
     - 验收标准: 文件保存成功，路径显示正确
  
  5. **场景5**: 文件夹自动创建
     - 输入: 首次使用保存功能
     - 期望输出: 自动创建`processed_documents`文件夹
     - 验收标准: 文件夹创建成功，无需手动创建

## 8. 用户界面设计建议

### 8.1 界面布局
```
┌─────────────────────────────────────────────────────────────┐
│  Word文档中英文空格清理工具                                │
├─────────────────────────────────────────────────────────────┤
│ [打开文档] [处理文档] [保存结果]                          │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────┐ ┌─────────────────────┐           │
│ │     原始文本        │ │     处理后文本       │           │
│ │                     │ │                      │           │
│ │                     │ │                      │           │
│ │                     │ │                      │           │
│ └─────────────────────┘ └─────────────────────┘           │
├─────────────────────────────────────────────────────────────┤
│ 统计信息: 段落数=10, 表格数=2, 文本项数=12                │
│ [处理进度条]                                              │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐   │
│ │ 详细信息表格                                        │   │
│ │ 原始文本 | 处理后文本 | 移除空格数 | 变更详情        │   │
│ └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 8.2 功能设计
1. **文件操作**
   - 拖拽支持：支持拖拽文件到窗口
   - 批量选择：支持选择多个文档
   - 文件加载：显示文档基本信息和文本预览

2. **处理控制**
   - 开始/暂停/取消：提供处理控制
   - 进度显示：实时显示处理进度
   - 结果展示：处理完成后显示对比结果

3. **结果展示**
   - 文本对比：并排显示原始和处理后文本
   - 统计信息：显示处理统计
   - 详细信息：表格显示每个文本段的处理情况

4. **保存功能**
   - 默认保存路径：自动保存到项目目录下的`processed_documents`文件夹
   - 相对路径显示：保存成功后显示相对路径，提升用户体验
   - 自动创建文件夹：如文件夹不存在则自动创建

### 8.3 用户体验优化
- **响应式设计**: 界面适应不同屏幕尺寸
- **快捷键支持**: 提供常用功能的快捷键
- **状态提示**: 清晰的状态信息和错误提示
- **撤销功能**: 支持撤销处理操作

## 9. 部署和分发

### 9.1 环境要求
- Python 3.7或更高版本
- Windows/macOS/Linux操作系统
- 2GB以上内存

### 9.2 安装步骤
```bash
# 1. 克隆项目
git clone <repository-url>
cd word-space-cleaner

# 2. 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate    # Windows

# 3. 安装依赖
pip install -r requirements.txt

# 4. 运行程序
python main.py
```

### 9.3 打包分发
使用PyInstaller创建独立可执行文件：
```bash
# 安装PyInstaller
pip install pyinstaller

# 打包程序
pyinstaller --onefile --windowed main.py

# 输出文件在dist目录
```

## 10. 维护和扩展

### 10.1 代码维护
- **代码规范**: 遵循PEP 8编码规范
- **文档更新**: 保持文档与代码同步
- **版本控制**: 使用Git进行版本管理
- **持续集成**: 设置自动化测试和构建

### 10.2 功能扩展
- **多语言支持**: 扩展支持其他语言组合
- **格式支持**: 支持更多文档格式（PDF、TXT等）
- **自定义规则**: 允许用户定义清理规则
- **批处理优化**: 优化大批量文档处理性能

### 10.3 性能优化
- **算法优化**: 优化边界检测算法
- **内存优化**: 减少内存占用
- **并行处理**: 支持多线程/多进程处理
- **缓存机制**: 缓存频繁处理的结果

## 11. 总结

本工具通过精心设计的中英文边界识别算法，能够准确识别并清理Word文档中英文之间的多余空格，同时保持英文单词间的正常空格。系统采用模块化设计，具有良好的可扩展性和维护性。

主要特点：
- **精准识别**: 基于Unicode的中英文边界检测
- **智能清理**: 仅清理目标位置的空格
- **格式保持**: 保持原文档的样式和结构
- **用户友好**: 直观的图形界面和详细的处理信息
- **可靠测试**: 完善的测试用例和错误处理

该工具能够有效提高中英文混排文档的质量，适用于各种文档处理场景。